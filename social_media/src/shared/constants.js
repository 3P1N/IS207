
export const CALL_STATUS = {
  IDLE: 'idle',           // Rảnh
  CALLING: 'calling',     // Mình đang gọi đi (chờ bên kia bắt máy)
  INCOMING: 'incoming',   // Có người gọi đến (đổ chuông)
  CONNECTED: 'connected', // Đang trong cuộc gọi
  ENDED: 'ended',         // Cuộc gọi kết thúc
};